// Common utilities for the chat application - Utils version
// Este archivo es redundante. Usar utils/helpers.js en su lugar.
// Mantener solo para compatibilidad hacia atrás.

if (!window.ChatHelpers) {
    console.warn('ChatHelpers no está disponible. Carga utils/helpers.js primero.');
}